services:
  discord-bot:
    image: registry.digitalocean.com/${REGISTRY_NAME}/discord-bot:latest
    restart: unless-stopped
    environment:
      - DISCORD_TOKEN=${DISCORD_TOKEN}

networks:
  bot-network:
    driver: bridge